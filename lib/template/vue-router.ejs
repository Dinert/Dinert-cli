// 普通加载路由
// import <%= data.name %> from './index.vue'

// 懒加载路由
const <%= data.name %> = () => import( /* webpackChunkName: "<%= data.name %>" */ './index.vue')

export default{
    path: '/<%= data.lowerName%>',
    name: '<%= data.name %>',
    icon: '<%= data.icon %>',
    meta: {
        requireAuth: true,
    },
    component: <%= data.name %>,
    children: []
}